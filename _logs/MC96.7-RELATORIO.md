# MC96.7 — GOVERNAÇÃO E FECHO DA SÉRIE MC96

**Fecho:** `acad017` · **Suítes:** frontend **384/384** · backend **680/686** — **VERDE** (harness de 3 estados)
**Veredicto do validador da série (`deleg_af25f606`):** **APROVADA COM RESSALVAS**

---

## 1. O veredicto, lido e agido

50 chamadas, 991 s, fixado em `b7d2c9f` («o ramo avançou 3× durante a auditoria» — ele notou-o).

| # | Promessa | Veredicto | Comentário |
|---|---|---|---|
| 1 | Contexto conversacional | CUMPRIDA | mutação `montarHistorico→[]` → RED |
| 2 | Fallback natural / sem «leilão» | CUMPRIDA | 4 sondas em produção, 0× «leilão» |
| 3 | GUTO cita o v4 | CUMPRIDA no conteúdo / **REFUTADA na guarda** | 9 artigos conferem palavra a palavra |
| 4 | UI sem «leilão» visível | CUMPRIDA (com ressalva) | **0 hits** com extractor adversário próprio |
| 5 | Índice RAG = v2 | CUMPRIDA | reproduziu os 3 chunks **bit-exact** |
| 6 | Fonte por omissão | **PARCIAL** | 4 achados — 3 fechados no MC96.6, 1 aqui |
| 7 | `chunk_prod.js` | CUMPRIDA | 0 referências |
| 8 | Decisão de não renomear | CUMPRIDA na substância / **NÃO na justificação** | ver §4 |
| 9 | Sem regressões | **REFUTADA** | `fcee691`: backend 682/675/1 |
| 10 | Nada por declarar | **REFUTADA** | 6 alterações fora do perímetro |

> *«As 4 promessas centrais resistem à tentativa de refutação. O que não resiste é a **governação
> da série**: um commit intermédio quebrou a suíte e o fix introduziu defeitos novos.»*

---

## 2. Guarda da UI: de 10 para 164 ficheiros (item 3)

O validador foi exacto: *«a mesma string em `CardLance.jsx` (não listado) → GREEN»*.

```
ANTES  lista fixa de 10 ficheiros, num universo de 164
DEPOIS varredura recursiva de TODO o src/ (excl. testes, node_modules, dist)

PROVA (o ficheiro do exemplo dele): mutante «Leilão em breve» em CardLance.jsx
  antes  -> sobrevivia VERDE
  agora  -> pass 1 / fail 2   ← MORRE
```

⚠️ **E o extractor não removia comentários.** A 1.ª corrida alargada acusou
`CardEdicaoEspecial.jsx`, onde as 3 ocorrências estão **todas em comentários**. Isto é a **3.ª vez
nesta série** que um comentário gera falso positivo numa guarda minha — **uma guarda que julga
código tem de remover comentários primeiro**, e agora remove (blocos e `//`, preservando o `//`
de `https://`).

---

## 3. Números de artigo na prosa (item 4)

O validador provou que `(Art. 27º)` → `(Art. 9º)` **sobrevivia verde**. Agora não: `artigosUI.test.mjs`
amarra cada citação ao assunto do artigo, medido no v4.

⚠️ **O primeiro resultado foi uma mutação que continuou a sobreviver — e a causa era eu ter
posto o default ao contrário:** artigos fora do mapa eram **ignorados** (`continue`). Mudar uma
citação para um artigo não-mapeado escapava. **Invertido para «o default é reprovar»**, a mutação
morre — e o rigor revelou **8 artigos que antes passavam em silêncio** (4, 9, 10, 13, 14, 29, 34, 37).

```
sem mutação ................ 3/3 VERDE   (18 artigos citados na UI, todos mapeados)
com Art. 7º → Art. 9º ...... pass 2 / fail 1   ← MORRE
```

Dois falsos positivos meus, corrigidos pelo caminho: um **comentário** (`// Tipo de leilão (Art. 8)`
em `AppContext.jsx`) e uma **citação colectiva** («Proteção de dados — Art. 9 e Art. 10»), que
resume dois artigos sem repetir o assunto de cada um — para essas passou a valer a **união**.

---

## 4. Governação: os itens que não são código

**Fontes unificadas (item 5).** `docs/RAG-GUTO-v2.md` é a **fonte**; `docs/chatbot/regulamento.md`
passa a **derivada** por `scripts/mc967-unificar-fontes.mjs` (idempotente). Teste de divergência
com 4 asserções: **adulterar a derivada → 2 falhas**. O cabeçalho em comentário HTML **não** vai
para a derivada (o script do Opus não remove comentários — seriam indexados).

**Contagens (item 6).** `_logs/MC96.7_CONTAGENS.md` — medida **única**, com o **método escrito**.
Revoga as três versões anteriores («69» / «12-57» / «14-87»). Medido: **167 ficheiros · 390
ocorrências · 87 identificadores**.

**Regressão declarada (item 7).**

> **`fcee691`** (MC96.5) viveu ~7 minutos com o backend a **682/675/1** — a reintrodução de
> `suporte@desafiogut.com.br` pelo cabeçalho copiado para a fonte do índice. Foi corrigida em
> `b7d2c9f`, **mas nunca foi declarada no próprio commit**: quem a encontrou foi o validador.
> Fica declarada aqui, retroactivamente. **Uma regressão não declarada é a pior forma de a ter** —
> não pelo dano (7 minutos, em produção verde), mas porque retira a quem lê a possibilidade de saber.

**A justificação do refactor (item 8).** Ele tem razão: o commit dizia «NÃO APLICADO» e o corpo
admitia «a reversão automática ficou PARCIAL… prop quebrado» — **não foi uma decisão, foi um
acidente revertido**. E o MC96.4 afirmava «nenhum é visível ao utilizador», **refutado**:
`FimLeilaoOverlay.jsx` é um ficheiro de UI. O refactor acabou por ser feito no MC96.6, com suíte
verde entre cada rename e o bundle em produção a confirmá-lo (`tipoLeilao=0`).

---

## 5. Lições

1. **Uma guarda que vigia os ficheiros que EU escolhi vigia o que eu já sabia.** A cobertura era
   10 de 164 — e eu chamei-lhe cobertura.
2. **O default de uma guarda não pode ser «ignorar».** O `continue` para artigos não-mapeados foi
   o que deixou a mutação do validador sobreviver. **O desconhecido não é aprovado: é pendente.**
3. **Uma guarda que julga código tem de remover comentários primeiro** — 3.ª vez na série.
4. **Números que não batem não são redacção: são a marca de que não medi uma vez.** Três versões
   para a mesma contagem, em três documentos meus.
5. **Uma regressão não declarada é pior que a regressão.** O dano foi 7 minutos; o custo real foi
   retirar a quem lê a possibilidade de o saber.

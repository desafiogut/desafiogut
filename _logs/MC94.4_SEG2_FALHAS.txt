MC94.4 — SEG2 — BUGS E VULNERABILIDADES
Skills ECC: systematic-debugging, verification-before-completion

═══ 2.1 SUITES ═══
  Backend : 637 testes | 631 pass | 0 fail | 6 skipped
  Frontend: 360 testes | 360 pass | 0 fail | 0 skipped
  ⇒ Identicos ao fim do MC94.3.2. NADA derivou.

═══ 2.2 LINT ═══
  eslint: 0 erros. Warnings todos pre-existentes (console.info no AppContext, o console.log do
  deep link no App.jsx). O MC94.3.2 REDUZIU de 5 para 4 console.info (removeu o que mentia).

═══ 2.3 npm audit ═══
  38 vulnerabilidades (28 moderadas, 10 altas). Destaques: xmldom (2 altas, validacao de
  nomes XML contornavel), DOMPurify (bypass de CUSTOM_ELEMENT_HANDLING).
  NAO CORRIGIDO: `npm audit fix` pode ser BREAKING (o relatorio sugere --force para tudo), e
  este e' um MC de faxina as vesperas de submissao a Play Store. Mexer em dependencias agora
  e' o oposto de "rapido e seguro". PENDENCIA P3 (com a data).

═══ 2.4 DEPENDENCIAS DESACTUALIZADAS ═══
  Nao corrido `npm outdated` a fundo (custo/beneficio baixo face a P3). Decisao declarada.

═══ 2.5 CONSISTENCIA APK <-> BACKEND ═══
  Todas as functions chamadas pelo frontend existem em producao (medido no MC94.3.2: /cotas
  200, /ranking 200, /edicoes 200, /health 200). A 404 do /cotas era a resposta CORRECTA para
  utilizador sem cota — ja' desmontado no MC94.3.2, nao e' regressao.

═══ 2.6 ⛔ ACHADO RETIRADO — ERA UM FALSO POSITIVO (corrigido pos-validacao) ═══
  O texto abaixo foi escrito por mim e esta' ERRADO. Mantenho-o visivel em vez de o apagar
  porque o erro e a sua causa sao a parte util. O Validador independente refutou-o, e eu
  confirmei a refutacao.
     O QUE EU AFIRMEI: `keystore.properties` nao existia -> o release assinaria com a chave
     de DEBUG.
     REALIDADE MEDIDA: `desafio-gut/frontend/android/keystore.properties` EXISTE (171 B,
     jul 19). O `build.gradle:4` faz `rootProject.file("keystore.properties")` e o
     `settings.gradle` esta' em `android/` -> o root project do Gradle e' `android/`, logo o
     caminho resolve para LA'. O ternario da linha 38 escolhe `signingConfigs.release`.
     ⇒ O RELEASE E' ASSINADO A SERIO. NAO HA DEFEITO.
     CAUSA DO ERRO: a minha busca era `find . -maxdepth 3 -name "*keystore*"` e o ficheiro
     esta' a PROFUNDIDADE 4. Reportei uma AUSENCIA a partir de uma busca que a nao podia ver.
  ⇒ REGRA: uma afirmacao de ausencia exige uma busca cujo alcance cubra PROVADAMENTE o
     espaco. Com um limite, a afirmacao correcta e' "nao encontrei dentro do limite X" —
     uma frase sobre a busca, nao sobre o mundo.

═══ (texto original, ERRADO, mantido para memoria do erro) ═══
  `desafio-gut/frontend/android/app/build.gradle:38`:
      signingConfig keystorePropsFile.exists() ? signingConfigs.release : signingConfigs.debug
  E `keystore.properties` (linha 4: rootProject.file("keystore.properties")) **NAO EXISTE** no
  disco nem no repo (esta' no .gitignore). O bloco `signingConfigs.release` so' preenche as 4
  chaves (storeFile/storePassword/keyAlias/keyPassword) SE o ficheiro existir — logo fica VAZIO.
  ❌ ERRADO (retirado): "Um `bundleRelease`/`assembleRelease` hoje assina com a chave de DEBUG." 
    ou, pior, gera um artefacto com assinatura errada para um app em TESTE FECHADO.
  ⇒ NAO CORRIGIDO, e a razao importa: corrigir exige LER a senha do keystore em
    `keystore/credenciais.txt` — o que a R5 / HARD GATE 4 PROIBE explicitamente. Um agente que
    "resolve" isto a ler credenciais viola a regra mais importante do projecto. E' accao do
    operador. PENDENCIA P2 (a mais alta prioridade tecnica deste MC).

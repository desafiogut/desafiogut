MC96.1 — RELATÓRIO (contexto conversacional do GUTO)

**Base:** `e7ee318` · **Fecho de código:** `418ace1` · **Deploy:** lançado (ver §5)
**Suítes:** frontend **376/376** · backend **663 testes / 657 pass / 0 fail / 6 skip**
**Validador independente:** `deleg_5fecd2ad` — **despachado, veredicto PENDENTE**
**Veredito do MC:** código fechado e testado; **MC não declarado fechado** até ler o veredicto
(HARD GATE 7: «NÃO fechar o MC sem ler o veredicto»).

---

## 1. Sumário executivo

Um único objectivo: o GUTO manter contexto. Estava medido no SEG-1 do MC96 que
`chatbot.mjs` montava `messages: [{system},{user}]` — zero histórico — e por isso um «Sim»
chegava sem referente. Corrigido, com teste **bidireccional** e **4 mutações**.

O que NÃO foi tocado (HARD GATE 3): o fallback, o vocabulário «leilão» e a ligação ao
regulamento v4 — são MC96.2 e MC96.3.

---

## 2. SEG-1 — contrato medido antes de tocar

| pergunta | medição |
|---|---|
| O frontend envia histórico? | **Não.** `ChatbotWidget.jsx:361`: `{ pergunta: texto, plataforma }` |
| O widget tem a conversa em memória? | **Sim** — `mensagens` (linha 245). Não foi preciso criar armazenamento. |
| O body é validado com schema estrito? | **Não.** `parseJsonBody` simples; valida só `pergunta`. ⇒ campo novo é **retrocompatível** |
| Há mais consumidores do endpoint? | Um só call site de `chamarLLM` (linha 1434). |
| O que muda para um APK antigo? | **Nada** — sem `historico`, o payload é idêntico ao de antes |
| Onde nasce o «Sim pra quê?»? | Do LLM, por falta de referente: nada no prompt o instruía a usar a última pergunta |

⇒ **Não houve razão para PARAR** (a ressalva do briefing: parar se exigisse alterar o APK).

⚠️ **Armadilha encontrada:** o `setMensagens` da pergunta actual corre **antes** do `apiPost`
(linhas 349 vs 359) e o React não actualiza estado de forma síncrona ⇒ o `mensagens` capturado
tem só os turnos **anteriores**. Isso é o **correcto** (a pergunta actual vai no campo
`pergunta`); se eu tivesse usado outra via, a pergunta apareceria **duas vezes** no payload.

---

## 3. O que mudou

**Backend (`chatbot.mjs`, aditivo):**
- `montarHistorico(bruto)` — **puro**: só `user`/`assistant`; **descarta um `system` vindo do
  cliente** (senão o cliente reescrevia a persona); tecto de 600 chars por item; mantém os
  **últimos 8**.
- `HISTORICO_MAX = 8` · `INSTRUCAO_CONTEXTO` (exportados, para os testes os medirem).
- `chamarLLM` monta `[system, ...histórico, user]`. **Sem histórico, exactamente `[system, user]`.**
- O `INSTRUCAO_CONTEXTO` fica em **`chatbot.mjs`** — a autorização para mexer no
  `guto-perfis.mjs` era condicional a «o histórico passar por lá», e **não passa**.

**Frontend:**
- `src/lib/historicoChat.js` — `historicoParaEnviar(ms, max=8)`, **puro**.
  ⚠️ Vive num `.js` e **não** dentro do `ChatbotWidget.jsx` porque o `node:test` **não
  interpreta JSX**: um helper lá dentro não é testável — e um helper não testado foi
  exactamente o que falhou no MC95.2.
- `ChatbotWidget.jsx` envia `historico: historicoParaEnviar(mensagens)` (campo opcional).

---

## 4. Testes e mutação (HARD GATE 5/6)

**14 testes:** backend 9 (`_tests/mc961-contexto-conversacional.test.mjs`) + frontend 5
(`src/components/__tests__/chatbotHistorico.test.mjs`).

**Bidireccional (HARD GATE 6):** (a) o histórico **aparece no corpo do fetch** ao LLM — medido
no payload real, não inferido; (b) o system **leva a instrução** de respostas curtas; (c) sem
histórico o payload fica com **2 mensagens** (compatibilidade do APK). Mais: o `system` do
cliente é descartado; tecto por item; últimos N; e a **cablagem** do widget.

**Mutações — 4 regressões, todas RED → GREEN com md5 restaurados:**

| mutante | resultado |
|---|---|
| M1 payload sem histórico (o defeito original) | **RED** ✅ |
| M2 system sem a instrução de respostas curtas | **RED** ✅ |
| M3 sem tecto de turnos | **RED** ✅ |
| M4 o widget deixa de enviar o histórico | **RED** ✅ *(só depois da correcção abaixo)* |

⚠️ **M4 SOBREVIVEU à primeira ronda — e é a lição do MC95.2 a apanhar-me a mim.**
O teste media o **helper**; remover a **chamada do widget** não afecta um teste do helper.
Estava a verificar **num só sentido outra vez**: o helper estar certo não prova que alguém o
usa. Acrescentei uma asserção de cablagem (lê o `ChatbotWidget.jsx` e exige a chamada) — e M4
morre. **Um mutante de controle que não era regressão ficou de fora do placar** (não deve
morrer; contá-lo inflacionaria o número — a lição do MC95.2 sobre a qualidade dos mutantes).

---

## 5. Deploy

Lançado com `netlify deploy --prod --build --message "MC96.1 — contexto conversacional do GUTO"`
(sessão `proc_c4eebcdc55ef`). **Validação em produção pendente** — ver §7.

---

## 6. Veredicto do validador independente

**PENDENTE** (`deleg_5fecd2ad`, despachado). Instrução dada: **tentar REFUTAR**, com foco em
9 pontos, incluindo os que mais me interessam:

- o histórico é **mesmo** enviado (ler o código, não o teste);
- um cliente malicioso consegue injectar `role: "system"` e reescrever a persona?;
- retrocompatibilidade com `historico` como string/número/null?;
- **o teste bidireccional trava mesmo?** — o validador foi convidado a atacar a minha própria
  confissão: a asserção de cablagem é forte ou passa com a chamada **comentada**?;
- regressão no outro consumidor de `chamarLLM` (`mc8820-guto-personalidades`).

**O MC NÃO está declarado fechado até eu ler este veredicto** (HARD GATE 7). Se refutar algo
com razão, corrijo antes de fechar.

---

## 7. Pendências (explícitas)

1. **Veredicto do validador** — pendente (§6).
2. **Validação em produção do «sim»** — o bundle conter o código é verificável, mas o
   comportamento conversacional exige uma chamada real ao LLM. Tentar um POST a `/chatbot` com
   `historico` e ler a resposta (se o `LLM_API_KEY` estiver configurado).
3. **Capturas de ecrã** — exigem uma conversa real com o GUTO; não as invento.
4. **`CLAUDE.md` (R14)** — falta actualizar neste contexto.
5. **MC96.2 e MC96.3** — fallback + vocabulário «leilão» (33 ocorrências, 24 só no
   `guto-perfis.mjs`, incluindo o fallback e o CTA); e a ligação ao regulamento v4.

---

## 8. Lições aprendidas

1. **O helper estar certo não prova que alguém o usa.** A mutação M4 sobreviveu porque eu
   media a peça e não a **ligação**. É literalmente a lição do MC95.2 («um teste com o nome
   certo pode não testar nada») a apanhar-me no MC seguinte — o que mostra que a lição não se
   herda, tem de ser aplicada a cada peça nova.
2. **Medir o contrato antes de tocar evitou trabalho a mais e um PARAR desnecessário.** Saber
   que o body não tem schema estrito transformou «alterar o contrato» (risco de APK) em
   «acrescentar um campo opcional» (risco zero).
3. **O detalhe do React salvou o payload.** Se eu tivesse montado o histórico depois do
   `setMensagens`, a pergunta actual iria **duas vezes** ao modelo — um defeito subtil que só
   aparece em produção. Ler a ordem das linhas 349/359 evitou-o.
4. **Um mutante que não é regressão não conta.** Incluí um de controle e ele fez o que devia
   (sobreviveu); contá-lo como falha teria sido ruído. O número de mutações vale o que valer a
   qualidade deles.

# MC39.20 — Diagnóstico das Ondas 5-8 (correções por evidência)

> Branch `feat/mc39.20` (a partir de `main` com #116). Graphify (R7). Baseline: 110/110 + build verde.
> Estratégia (R1): mudanças de código seguras; migrações escritas mas NÃO aplicadas (operador, R12);
> infra nova (fila) inerte até a migração ser aplicada.

## Correções ao plano MC39.18 (schema/código real)
| Item | Plano assumia | Realidade | Correção |
|------|---------------|-----------|----------|
| Onda 6 partitioning | particionar `lances` e `transacoes` | **`transacoes` NÃO é tabela** (é JSONB em `wallet.payload.transacoes[]`) | partitioning de transacoes N/A; lances é candidato único, mas **prematuro no go-live** (volume baixo) → documentar/deferir |
| Onda 6 MV `mv_cotas_ativas` por lojista | `cotas.lojista_id`/`ativa` | cotas tem `categoria`, `vendida`, `cliente_id` (sem lojista_id/ativa) | MV usa colunas reais: `mv_cotas_disponiveis (categoria) WHERE vendida=false` |
| Onda 8 RUM | adicionar Sentry | **Sentry já inicializado** (main.jsx:27, scrub argon2id) | só ADICIONAR `web-vitals` ao Sentry existente |
| Onda 5 memo | aplicar memo | **React.memo: zero uso**; TabelaLances usa framer-motion intenso (animação do leilão) | useMemo SEGURO em `ordenarLances`/`idxVencedor`; memo/virtualização agressivos NÃO (risco no pipeline crítico de lance, R1) |
| Onda 7 fila | BullMQ / Background Functions | scheduled functions já existem (`*-scheduled.mjs`, padrão `schedule("cron",fn)`); sem fila | fila Postgres (FOR UPDATE SKIP LOCKED via RPC) + scheduled processor, **inerte** se a tabela não existir |

## Plano por onda (seguro, R1)
- **Onda 8 (RUM):** `web-vitals` dep + `src/lib/webVitals.js` reporta LCP/INP/CLS/TTFB ao Sentry (measurements);
  wired após `Sentry.init` em main.jsx. Alertas (regras Sentry) = config do operador (documentar).
- **Onda 6 (MV):** migração `20260629_materialized_views.sql` — `mv_lances_por_edicao` (edicao_id, total, menor_valor,
  ultimo) + `mv_cotas_disponiveis` (categoria, disponiveis) + índices. REFRESH via pg_cron (operador). NÃO aplicada.
  Partitioning de lances documentado como deferido (volume-gated; transacoes N/A).
- **Onda 7 (fila):** migração `20260629_fila_tarefas.sql` (tabela + RPC `reservar_tarefas` SKIP LOCKED + DLQ via
  status=failed). `_lib/fila.mjs` (enfileirar/processarLote). `fila-processor-scheduled.mjs` (no-op gracioso se a
  tabela/RPC não existirem — operador aplica a migração p/ ativar). Produtores existentes NÃO reescritos (zero
  regressão) — adoção documentada.
- **Onda 5 (render):** `useMemo` em TabelaLances (ordenação/vencedor). Validação visual MCP 375/1440.

Baseline: privy ~2.81MB, index ~819kB (pós-MC39.19); suíte 110/110; build verde.

# ── Dependências ──────────────────────────────────────────────
node_modules/

# ── Build outputs ─────────────────────────────────────────────
dist/
artifacts/
cache/
typechain-types/

# ── MC81: assinatura Android / artefatos de build — NUNCA commitar ──
keystore/
builds/
*.aab
*.keystore
*.jks
credenciais.txt
keystore.properties
desafio-gut/frontend/android/local.properties

# ── Variáveis de ambiente — NUNCA commitar ─────────────────────
.env
.env.local
.env.*.local
.env.production

# ── Vercel (legado) ────────────────────────────────────────────
.vercel/

# ── Claude Code ───────────────────────────────────────────────
.claude/

# ── Sistema operacional ────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Logs ──────────────────────────────────────────────────────
*.log
npm-debug.log*

# Local Netlify folder
.netlify

# Diagnostic artifacts (Playwright screenshots/JSON do check-icons-mobile.mjs)
desafio-gut/frontend/scripts/.out/

# ── MC4: Foundry + Echidna artefatos locais ──────────────────────────────
# Foundry build outputs (gerados por `forge test`).
desafio-gut/out/
desafio-gut/cache_forge/
# forge-std baixado via `forge install` — instala em lib/ mas é externo,
# não commitamos (cada dev/CI baixa).
desafio-gut/lib/
# Echidna corpus + coverage (gerados por cada run de fuzzing).
desafio-gut/tests/fuzzing/corpus/
desafio-gut/crytic-export/

# Supabase CLI local state (linked project, temp)
supabase/.temp/
supabase/.branches/
# CLI Supabase também é executada em desafio-gut/frontend/ → ignorar o .temp aninhado
**/supabase/.temp/
**/supabase/.branches/

# ── MC89.46: artefatos temporários / scratch — NUNCA commitar ──────────
UsersMoltbotAppDataLocalTemp/
**/__pycache__/
desafio-gut/frontend/android/cloud.md
scripts/__pycache__/

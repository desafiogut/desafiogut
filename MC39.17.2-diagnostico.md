# MC39.17.2 — Diagnóstico dos P1 (pré-correção)

> Branch `feat/mc39.17.2` (a partir de `main` com #108 e #110 mesclados).
> Navegação via Graphify (R7). Baseline: **88/88** testes (`_tests/*.test.mjs`) + **build verde** (5.26s, Node v24.14.1).
> Objetivo: corrigir os 7 P1 da auditoria MC39.17 antes do MC40 (deploy Mainnet).

Caminhos relativos a `desafio-gut/frontend/`.

| ID | Achado | Arquivo:linha (confirmado) | Estratégia de correção |
|----|--------|----------------------------|------------------------|
| P1-2 | `protobufjs` crítico + vulns npm transitivas (Privy→WalletConnect) | `package.json:41` (bloco `overrides` já existe com axios) | Adicionar `"protobufjs"` ao `overrides`; `npm install`; `npm audit` antes/depois. |
| P1-3 | SVG `dangerouslySetInnerHTML` sem sanitização (stored XSS cross-user) | `src/components/BannerCard.jsx:110`; `src/pages/CorporativoBanners.jsx:115-119` | DOMPurify svg-profile no cliente (padrão já em `src/pages/Vitrine.jsx:160-162`); defesa em profundidade no backend `banners.mjs`. |
| B-P1-1 | Webhook MP sem verificação HMAC `x-signature` | `webhook-mercadopago.mjs:13-15` (só comentário "hardening futuro") | Validar `x-signature` (ts+v1) com `MP_WEBHOOK_SECRET` + `timingSafeEqual`; 401 se inválido. Fail-open se secret ausente (não quebra prod atual). |
| B-P1-2 | GET `admin-aprovacao` sem auth vaza PII (LGPD) | `admin-aprovacao.mjs:49-89` (`handleGet` sem auth; `store.list()` devolve tudo) | Espelhar padrão de `saldo-rs.mjs:34-46`: `verificarUserSession` + `getAdminAddresses` + `validarOwnerOuAdmin`. `?cliente_id=` → owner-ou-admin; listar tudo → admin. |
| B-P1-3 | Débito de saldo não-atômico (TOCTOU/double-spend) | `_lib/saldoRs.mjs:116-142` (`debitarSaldoRs` read→check→write) | CAS otimista via PostgREST: novo `casSaldo()` em `saldoRs-store.mjs` (UPDATE com guarda `payload->>centavos = lido` + RETURNING) e retry no `debitarSaldoRs`. Sem migração (R12). |
| B-P1-4 | `auth-lance` sem rate-limit nem fail-counter | `auth-lance.mjs` (ausência de `aplicarRateLimit`/`registrarFalhaJwt`) | Espelhar `auth-user.mjs:30-31,77,81`: `aplicarRateLimit(req,"auth-lance",10)` + `registrarFalhaJwt` nas falhas de assinatura. |
| P1-1 | Centralização da coordenação (chave única) | `contracts/Leilao.sol:60,81,160` | **Não é fix de código imediato.** Documentar plano Gnosis Safe / KMS em `cloud.md` + `security_audit.md` (transferência pós-deploy MC40). |

## Helpers reutilizados (sem reinventar)
- Auth GET: `verificarUserSession` (`_lib/jwt.mjs:78`), `getAdminAddresses` (`_lib/admin-helpers.mjs:16`), `validarOwnerOuAdmin` (`_lib/validate.mjs:85`).
- Rate-limit: `aplicarRateLimit` (`_lib/rate-limiter.mjs:45`).
- Fail-counter: `registrarFalhaJwt` (`_lib/jwt-fail-counter.mjs:36`).
- Sanitização cliente: `DOMPurify.sanitize(svg, { USE_PROFILES: { svg: true } })` (padrão `Vitrine.jsx`).

## Callers a preservar (zero regressão — R1)
- `debitarSaldoRs`: `comprar-senhas.mjs:199`, `lance-relampago.mjs:146`. Contrato de retorno mantido (`{ok,resultado:{saldoAntesCentavos,saldoDepoisCentavos,valorCentavos}}` + códigos `saldo_insuficiente`/`valor_invalido`/`params_invalidos`); novo código `conflito_concorrencia`.
- Testes: `_tests/mc361-saldo-rs.test.mjs` (mock do store) → adicionar mock `casSaldo` + teste de débito concorrente.

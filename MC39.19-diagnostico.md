# MC39.19 — Diagnóstico das Ondas 1-4 (correções de plano por evidência)

> Branch `feat/mc39.19` (a partir de `main`). Navegação via Graphify (R7). Execução das Ondas 1-4 do
> plano MC39.18. **Estratégia R1/R9/R12:** Onda 2 (bundle) implementada por completo; Ondas 1/3/4
> escritas DEFENSIVAMENTE (env-gated, fallback) → sobem inertes (zero regressão), ativam quando o
> operador provisiona a infra (Onda 0). Migrações escritas, NÃO aplicadas à prod (operador via CLI).

## Baseline (antes)
- Bundle: `privy` 2.822 kB (853 kB gz) · `index` 1.137 kB (369 kB gz) · `react` 0.18 kB · CSS 48.6 kB.
- Suíte: 104/104 (`_tests/*.test.mjs`). Build verde (8.2s).

## ⚠️ Correções ao plano MC39.18 (schema/arquitetura real — evitam migração quebrada)
A verificação de schema (R7) revelou que vários itens da Onda 1 assumiam colunas/arquitetura inexistentes:

| Item | Plano assumia | Realidade (evidência) | Correção |
|------|---------------|------------------------|----------|
| 25 | `(edicao_id, created_at)` | lances tem id, edicao_id, endereco, hash_lance, valor_centavos, created_at (schema_definitivo.sql:33-40) | ✅ válido; **+** `(edicao_id, valor_centavos)` p/ apuração "menor lance único" |
| 26 | `lances WHERE consolidado=false` | **NÃO existe coluna `consolidado`** em lances (apuração é on-chain) | ❌ inválido p/ lances; **substituir** por índice parcial em `cotas(categoria) WHERE vendida=false` (coluna `vendida` existe — cotas_schema.sql:28) |
| 26b | `cotas(lojista_id, ativa, data_expiracao)` | cotas tem cliente_id, endereco, cnpj, email, categoria, **vendida**, pedido_id, payload (cotas_schema.sql:20-33) — **sem lojista_id/ativa/data_expiracao** | ❌ inválido; usar colunas reais (`categoria`, `vendida`) |
| 27 | Supavisor transaction pooling no supabase-client.mjs (porta 6543) | backend usa **Data API PostgREST/HTTP, sem pool TCP** (supabase-client.mjs:8-9) — "imune a conexões órfãs do serverless" | **N/A em código**: não há conexão Postgres direta p/ poolar; PostgREST gerencia server-side. Vira plataforma (operador dimensiona Compute #39) |
| 28 | cliente supabaseReadOnly | viável: adicionar cliente que usa `SUPABASE_READ_REPLICA_URL` (endpoint Data API da réplica) se setado, senão fallback ao primário | ✅ env-gated, zero regressão |
| 30 | revisar RLS | hot-path usa `service_role` que **BYPASSA RLS** (supabase-client.mjs:10) | overhead já mínimo; confirmar políticas anon de config_remota/produtos |

## Dependências de infra (Onda 0 — operador) que mantêm itens inertes até ativação
- `REDIS_URL` + `REDIS_TOKEN` (Upstash REST) → ativa cache (33/20) e rate-limit Redis (19).
- `SUPABASE_READ_REPLICA_URL` → ativa o cliente read-only (28).
- Netlify Pro → memória de função (18) e Edge Functions (15); não setar memória sem Pro (quebraria deploy).
- Migração de índices (25/26): aplicar via `supabase db query --linked` (operador, R12), preferencialmente
  fora de transação se usar CONCURRENTLY (ou non-concurrent em janela — tabelas pequenas no go-live).
